{
  "name": "@foundation/cli",
  "packageManager": "yarn@3.5.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "x-build": "yarn clean && yarn compile",
    "xclean": "rimraf ./dist",
    "xcompile": "tsc --build",
    "xstart": "yarn node dist/index.js",
    "xrun": "ts-node ./src/index.ts",
    "xtest": "echo hello"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@foundation/core": "workspace:*"
  }
}
